name: Deploy Website

on:
  push:
    branches:
      - main
    paths:
      - 'index.md'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
      - name: Deploy
        run: echo "Deploying site..."
